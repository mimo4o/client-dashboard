@PROMPT.md

## Key Rules

1. Only edit `src/lib/project-data.ts` and `src/app/globals.css` (accent color lines 8-9)
2. NEVER modify component files in `src/components/`
3. Use Traditional Chinese (繁體中文) for all communication
4. Follow the 8-step intake flow in PROMPT.md
5. Ensure all technical requirements (exact counts, `as const`, etc.) are met
